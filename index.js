// const nums = [1,2,3];
// const squares = nums.map(x = x**2);
// squares; [1, 4, 9]
// nums; [1, 2, 3]

// In the following example, we can imagine that overdueTodoItems is a collection of DOM elements:
//finishedItems = overdueToDoItems.map(item => item.className = "complete");
// headers.innerText = `You finished ${finishedItems.length} items!`;

//Or we might use map in billing software:
//lapsedUserAccounts.map( u => sendBillTo(u.address) );

const divide = function(num1, num2) {
    return num1/num2;
}
divide(2000, 100);

const square = x => x * x;

const add = (x, y) => x + y;


